Modules in `auto/` are tied to existing nix modules and automatically set to
opinionated defaults and settings for the related features. Unless you are one
of the Introdus authors, probably want to disable loading of the auto settings
by setting `introdus.autoModules = false;`.
